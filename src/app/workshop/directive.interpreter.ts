import { IComputerDirective, IExpectations } from "../challenge.service"

//Check challenge.service.ts
//A computer command must make the computer do something
//by it the captainf expect something to happen
//disengage docking clamp THEN docking = false
//slowly engage engines THEN engine = 1
export default function interpret(directive : IComputerDirective) : IExpectations {
    return {

    };
}